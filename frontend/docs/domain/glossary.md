# Domain Glossary

Terms agents must understand before changing product or business logic in the
frontend. The **backend is the source of truth** for domain models and API
contracts — see `../backend/AGENTS.md` and the backend domain rules. Add a row
here whenever a product flow lands in the UI.

| Term | Meaning | Notes |
| --- | --- | --- |
| 가꿈 | The product this repository builds a frontend for. | Backend defines its domain model. |
| (add terms as flows land) | | |
